[metadata]
name = crowdparser
version = 0.1.0

[options]
package_dir = = src
packages = find:
python_requires = >=3.9

[options.packages.find]
where = src

[options.entry_points]
console_scripts =
    crowdparser = crowdparser.cli:main
